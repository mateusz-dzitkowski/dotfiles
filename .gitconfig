[user]
    name = mateusz.dzitkowski

[core]
    autocrlf = input

[credential]
    helper = store

[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
    clean = git-lfs clean -- %f

[pull]
    rebase = true

[include]
    path = ~/.gitconfig-local

